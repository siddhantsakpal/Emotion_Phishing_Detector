# Copy this file to .env and fill in your real values.
# NEVER commit .env to git — add it to .gitignore

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=emotion_phishing_dbs

# Set to "false" in production
FLASK_DEBUG=true
